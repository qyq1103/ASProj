import org.gradle.api.initialization.resolve.RepositoriesMode

import java.lang.reflect.Field

pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "ASProj"
include ':app'

//项目模块
include ':common'

//外部项目模块库
include(':library')
project(':library').projectDir = "../hilibrary/hi_library" as File
include(':hi-ui')
project(':hi-ui').projectDir = "../hiui/hi_ui" as File